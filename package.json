{
  "name": "style-dictionary-multi-brand-multi-theme",
  "version": "1.0.0",
  "description": "Multi-brand, multi-theme design tokens using Style Dictionary",
  "type": "module",
  "scripts": {
    "build": "node ./build.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "style-dictionary": "^5.3.3"
  }
}
